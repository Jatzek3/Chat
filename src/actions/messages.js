import { FETCH_MESSAGES, SEND_MESSAGE,SET_NAME } from './types' 

export const fetchMessages = () => dispatch => {
    console.log('connecting to server')
        //  Here we fetch data from Websockets - no longer need Component Will Mount
        // .then(data => dispatch( <-------
        // type : FETCH_MESSAGES,
        // payload: data)
        // 
    };
